apiVersion: v1
kind: Service
metadata:
  name: {{ include "notification-system-api.fullname" . }}
  labels:
    {{- include "notification-system-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notification-system-api.selectorLabels" . | nindent 4 }}
