apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-management-api.fullname" . }}
  labels:
    {{- include "order-management-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "order-management-api.selectorLabels" . | nindent 4 }}
