name: Docker Image CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'services/order-management-api/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'services/order-management-api/**'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file services/order-management-api/Dockerfile --tag order-management-api:$(date +%s)
